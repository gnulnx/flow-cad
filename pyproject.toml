[project]
name = "erb-cad"
version = "0.1.0"
description = "Parametric CAD and validation helpers for the Erb balance bot chassis"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
